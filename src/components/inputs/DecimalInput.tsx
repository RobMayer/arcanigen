import styled from "styled-components";
import { AbstractInput } from "../abstract/Inputs";

export const DecimalInput = styled((props: AbstractInput.NumericProps) => <AbstractInput.Numeric {...props} />)``;
