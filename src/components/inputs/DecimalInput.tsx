import styled from "styled-components";
import { AbstractInput } from "../abstract/Inputs";

export const DecimalInput = styled((props: AbstractInput.Numeric.Props) => <AbstractInput.Numeric {...props} />)``;
